package ainvoke

const (
	// InputFileName is the name of the file containing the input JSON.
	InputFileName = "input.json"
	// InputSchemaFileName is the name of the file containing the input JSON schema.
	InputSchemaFileName = "input.schema.json"
	// OutputSchemaFileName is the name of the file containing the output JSON schema.
	OutputSchemaFileName = "output.schema.json"
	// OutputFileName is the name of the file containing the output JSON.
	OutputFileName = "output.json"
)
